<!-- METODO-GATE: non togliere. Fonte: rockjoel/modo-agentico → docs/METODO-GATE.md -->

# CANCELLO METODO (leggi prima di qualsiasi modifica)

1. Questo progetto usa il **modo agentico** di Gioele.
2. **Prima** di spostare anche una virgola: apri l’hub  
   `https://github.com/rockjoel/modo-agentico`  
   (clone tipico: `%USERPROFILE%\AI Projects\modo-agentico\`)  
   e Read **`docs/METODO-GATE.md`** + **`metodo/PRINCIPI.md`**.
3. **Adotta** quel metodo. Non inventare un secondo stile di lavoro.
4. Se vuoi fare qualcosa **fuori** dal metodo → **avverti Gioele in una riga e stop**. Non procedere in silenzio.
5. Poi leggi il resto di **questo** file e l’indice del progetto (se c’è).

---

# Agenti — questo repo

Fonte unica del modo di lavorare (si cambia solo lì):

`%USERPROFILE%\AI Projects\modo-agentico`  
GitHub: https://github.com/rockjoel/modo-agentico

Vale per **ogni** harness (Cursor, Codex, Grok Bot, Claude, Cloud, esterni).

**Come va / aggiornami / dove siamo:**

1. Se manca indice o STATO → skill `bootstrap-agentico`. Stop sul prodotto.
2. Se c’è STATO → solo **Ripresa** in chat.
3. Fronte chiuso → skill `github-quando` (backup su **master**).

Skill già fatte: indice del progetto + hub `SKILL-INSTALLATI.md`. Una riga = un file.

Italiano. Maker, non programmatore. Lavoro: chiudi con `Prossimo:` (persona: no).
